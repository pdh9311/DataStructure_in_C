#include "radix.h"

void radixSort(ArrayList *arrayList)
{
	LinkedDeque **deque;
	DequeNode element;
	for (int i = 0; i < RADIX; i++) {
		deque[i] = createLinkedDeque();
	}

	for (int i = 0; i < arrayList; i++) {
		insertFrontLD()
	}

}